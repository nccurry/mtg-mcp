# Security

Report security issues privately to the maintainers.

Do not share Archidekt JWTs, refresh tokens, usernames, or passwords in issues. The server redacts known secret fields from config resources and logs, but callers should still treat MCP transcripts as sensitive when deck credentials are configured.
